load_module modules/ngx_http_htaccess_module.so;

events {
    worker_connections 1024;
}

http {
    server {
        listen 8080;
        root /var/www/site;

        location / {
            htaccess on;
        }
    }
}
